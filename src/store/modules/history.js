const state = {
  scene: 'common',
  sidebarOpen: false,
  currentId: "",
  refreshId: 1,
}

const mutations = {}
const actions = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
